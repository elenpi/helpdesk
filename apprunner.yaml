version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
      - pip install git+https://github.com/elenpi/helpdesk.git
run:
  runtime-version: 3.8.16
  command: sh startup.sh
  network:
    port: 8000